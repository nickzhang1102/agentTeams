# 旧迁移脚本废弃说明

本目录下的迁移脚本已被 **Flask-Migrate / Alembic** 替代，不再使用。

## 废弃原因

- **无版本追踪**：旧脚本无法追踪迁移历史，无法回滚
- **无依赖管理**：脚本执行顺序靠人工记忆，容易出错
- **无 CI/CD 集成**：生产部署风险高

## 新迁移方案

使用 Flask-Migrate（Alembic 的 Flask 封装）管理数据库迁移：

```bash
# 查看当前版本
flask db current

# 查看迁移历史
flask db history

# 生成新迁移脚本（自动检测模型变化）
flask db migrate -m "描述"

# 应用迁移
flask db upgrade

# 回滚迁移
flask db downgrade
```

## 保留脚本用途

这些脚本保留作为参考：
- `init_db.py` - 数据库初始化逻辑参考
- `migrate_add_*.py` - 特定字段添加逻辑参考
- `manual_migration.sql` - 复杂迁移 SQL 参考

**请勿直接执行这些脚本**，它们可能与当前数据库状态不一致。

---
**废弃时间**：2026-06-03
**替代方案**：Flask-Migrate (Alembic)